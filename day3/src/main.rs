fn main() {
    let input = include_str!("../input");
    let leads: Vec<(u32, u32)> = Vec::new();

    for (i, row) in input.split_whitespace().enumerate() {
        for (j, char) in row.chars().enumerate() {
            if !char.is_alphabetic() {

            }
        }
    }
}
